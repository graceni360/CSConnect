<h1>Employer Sign Up</h1>

<%= link_to "Back", login_index_path %>
<%= form_for :company do |f| %>

<% if @company.errors.any? %>
    <div id="error_explanation">
        <h2>
            <%= pluralize(@company.errors.count, "error") %> prohibited
            this employer account from being saved:
          
  </h2>
        <ul>
            <% @company.errors.full_messages.each do |msg| %>
          <li>
      <%= msg %>
    </li>
            <% end %>
  </ul>
      
</div>
  <% end %>
    
<p>
  <%= f.label "Representative Email" %><br>

    <%= f.text_field :companyEmail %>

  </p>

<p>
  <%= f.label "Company Name" %><br>

    <%= f.text_field :name %>

  </p>

  <%= f.submit "Submit" %>

</p
      
      <% end %>
        
        